#!/usr/bin/env bash
# Render the local Cortex markdown dashboard.

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${SCRIPT_DIR}/cortex_dashboard_md.py" "$@"
