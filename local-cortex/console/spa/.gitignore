# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# dist is the BUILT SPA bundle — DELIBERATELY TRACKED (the native redistributable ships it; a
# fresh npm-less server can't rebuild it). See the repo-root .gitignore note. (was: `dist`)
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
